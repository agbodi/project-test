name: My tests workflow

on:
  push:
  pull_request: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
       - run: echo "First workflow was a success"
  prod:
    runs-on: ubuntu-latest
    steps:
       - run: echo "Second workflow was a success"
